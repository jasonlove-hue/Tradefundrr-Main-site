# Marketing Site Session Handoff — 2026-06-03

**For:** the next Claude instance, or Jason on a future revisit
**Repo:** `C:\Users\Trader\OneDrive\Desktop\TradeFundrr Brain\main-site\`
**Deploys to:** https://tradefundrr-main-site.vercel.app/ (NOT `tradefundrr.com`, still old WordPress)
**Previous handoff:** `HANDOFF-2026-06-02.md`

**Theme of the day:** implemented Nadav's T3 "foundation-stack" restructure sitewide; reordered the futures page for proof-forward flow; a compliance + copy pass on futures; replaced the unverifiable switcher offer; standardized the site on Geist Mono; rebuilt the futures payout-rain into a cadence-led "Paid. Every Friday." section (long iteration — landed by replicating the homepage card system exactly, recolored blue); applied the same payout layout to the homepage; delivered a full critical marketing/compliance review of the futures page; and prototyped a Clear Street-style scroll animation for the stocks hero (mockups only, not live).

> **Workspace rules still apply:** the agent CANNOT git push (no creds) — Jason pushes. Do NOT read/write the big HTML files via bash/cat/python/sed (OneDrive mount serves stale/truncated copies) — use `Read`/`Write`/`Edit`; `grep`/`git` via bash are fine. Mockups (`*-mockup.html`) are `.gitignore`d scratch in `main-site/` root.

---

## TL;DR — what shipped today (live unless noted)

### T3 "foundation-stack" restructure (Nadav 2026-06-02 sync) — sitewide
1. Replaced the old `.t3-ents` two-column "equals" layout with a **vertical foundation stack**: **T3 Global on top** ("Where you graduate") → **"▲ Built on the same infrastructure"** connector with three chips (trading platforms · compliance team · system-ops team) → **T3 Trading Group as a full-width foundation slab** ("The foundation") with the SEC/FINRA/SIPC creds. Applied to `index.html`, `pages/stocks.html` (teal), `pages/futures.html` (blue).
2. **About** (`pages/about.html`): the "Partnered with" list reframed — T3 Trading Group = "the foundation," T3 Global = "built on it" (no `.t3-ents` section there).
3. The **"trade desk" chip was removed** per Jason (not in the chips or the foundation-card prose). All four disclaimers preserved verbatim. No em-dashes. Homepage reveal migrated off `nth-child` → `.t3-stack`. Design System §14 marked **IMPLEMENTED**.

### Futures page (`pages/futures.html`) — big pass
4. **Proof-cluster reorder:** moved **T3 + Payouts + Reviews** above FAQ + business-model. Flow is now hero → "already funded" lead → mechanics spine → them-vs-us → Programs → Big 5 → switcher → **T3 → Payouts → Reviews** → FAQ → business model → platforms → who-it's-for → CTA. (Done by relocating the FAQ+business-model block below Reviews.)
5. **Compliance + copy:** hero subhead and final CTA now carry **"simulated"** next to the big claims; final-CTA headline **"Start trading our capital" → "Start trading a funded account"**; the two stacked closers were **merged** (deleted the grafted "Stop renting evaluations" closer, kept the richer final CTA); added a **footnote explaining the per-session profit cap** in the spec table.
6. **Switcher card:** the unverifiable **"Switcher offer"** (needed an account-screenshot verification we can't run, and linked to a dead `/pages/futures/switch`) was replaced with a **"The structural difference"** card — concrete, on-page terms only, CTA → `#programs`. The two dead `/pages/futures/switch` links now anchor to `#switch` (the "Coming from another firm?" section got `id="switch"`).
7. **Removed the "switch anytime" platform-switching language** (both the sub and the bolded note).
8. **Payout-rain section rebuilt → "Paid. Every Friday."** Cadence-led centerpiece in a frosted panel (kicker → "Paid. Every Friday." → sub → stat chips **Keep 80% · Direct to your account · No delays · ★4.3/67+** → disclaimer). **Final implementation replicates the homepage card system verbatim** (single layer, original `1.webp`–`21.webp`, `drop-shadow` filter, sizes 280/360/420, `payoutRain` keyframes, frosted center panel) **recolored blue via `hue-rotate(40deg)`.** Also: killed the inherited green `payouts-cards.webp` background (`background-image:none !important`), copy says "straight to your **account**" (dropped "Rise"), "You keep 80%" on its own line.

### Homepage (`index.html`)
9. **Numbers section → "Paid. Every Friday."** Swapped the four-line stack for the same cadence centerpiece + stat chips, **teal** (panel narrowed to 920px). Falling-card rain underneath unchanged.

### Site-wide
10. **Monospace standardized on Geist Mono.** `pages/stocks.html` + `pages/futures.html` switched off **JetBrains Mono** (self-hosted `@font-face` 400/500/600 + preload added; JetBrains dropped from the Google Fonts request). Fixes the futures SVG chart labels that hardcoded "Geist Mono" but weren't loaded. Other pages already used Geist.

### Analysis delivered (in chat, not a file)
11. **Full critical marketing + compliance review of the futures page** — see "Outstanding" for the action items it surfaced.

### Stocks hero scroll animation — PROTOTYPE ONLY (not live)
12. Recreated Clear Street's hero scroll effect. Two directions exist as **gitignored mockups**:
    - **Simple tilt-to-flat:** the dashboard image tilts ~30° and stands up flat on scroll. *(This version's CSS+JS was added to `pages/stocks.html` but is **NOT pushed** — Jason asked to see a mockup first: `stocks-hero-tilt-mockup.html`.)*
    - **Pieced-out "assemble":** the dashboard is built from separate panels (equity, P&L, payout, buffer, profit-split donut) that start scattered/tilted in 3D and **fly together** while the hero is **pinned**. Mockups: `stocks-hero-assemble-mockup.html` (stacked) and `stocks-hero-assemble-sidebyside-mockup.html`. Pattern documented in Design System §15.

---

## ⚠ Outstanding Items (do these next)

1. **Decide the stocks hero animation & build it live.** Pick stacked vs side-by-side assemble (or the simple tilt). The simple-tilt CSS/JS is currently sitting in `pages/stocks.html` **unpushed** — if you ship the assemble version instead, replace/remove that code. The assemble version needs **approved, "simulated"-safe panel values/labels** (mockup numbers are placeholders) before going live.
2. **Dead CTAs on futures** (Jason said he'd handle): program-card buttons ("Claim the $19 promo," "Choose Growth," "Choose Express"), hero "Talk to our team," and the final-CTA buttons are still `href="#"`. Wire to the real checkout/dashboard flow.
3. **Futures footer relative-path bug (from the review, NOT fixed).** The footer links use `href="pages/why-us.html"`, `pages/terms.html`, etc., but the page lives in `/pages/` — so they resolve to `/pages/pages/...` (404). That means **Terms / Privacy / Cookie / Disclaimer / Impressum are unreachable from the futures footer.** Also "Terms and Conditions" → `pages/terms.html` and `terms.html` may not exist. Fix the paths (and "Contact Us" → about.html mislabel).
4. **Delete dead scratch images:** `assets/images/payouts-tight/` (`p1–p21`, `k1–k21`, `1–21`/cropped, `_sim_*.jpg`, `_test1.webp`) is **no longer referenced** — the final futures payout-rain uses the original `assets/images/N.webp`. Safe to delete the whole `payouts-tight/` folder.
5. **Confirm the per-session-cap footnote wording** matches the real rule (I wrote a sensible definition from the spec label; verify before relying on it).
6. **Compliance review pending on Christian's stocks "downside capped" script** (if/when used): "Your upside? **Unlimited**." and "**$0** of your money at risk" are earnings-style / absolute claims that need a compliance look and the "simulated / only the eval fee at risk / standard fees apply" guardrails.
7. **Carryover from 2026-06-02:** options page = stocks page text-swapped; crypto still routes to `pricing.html` (no dedicated page); orphaned dead CSS (`.bu-*`, `.eq-*`, `.hiw-*`, `.rev*`); pre-existing em-dashes in stocks.

---

## Push commands (Jason runs these)

Most of today's work was given as per-change push commands and is believed deployed (live screenshots confirmed the T3, payout-rain, homepage, and mono changes). The **stocks hero tilt is the one explicitly not pushed.** A catch-all for anything still uncommitted:

```
cd "C:\Users\Trader\OneDrive\Desktop\TradeFundrr Brain\main-site"
git add index.html pages/stocks.html pages/futures.html pages/about.html
git commit -m "2026-06-03: T3 foundation-stack sitewide; futures proof-reorder + compliance copy + structural-difference card; mono→Geist; payout-rain 'Paid. Every Friday.' (futures+homepage)"
git push origin main
```

Design-system + messaging docs live in the vault (outside the `main-site` repo): `../07 Systems/TradeFundrr Landing Page Design System.md` (§15 added) and `../04 Content/HOMEPAGE-DESIGN-MESSAGING.md` (06-03 note).

After Vercel rebuilds (~30–60s), hard-refresh (Ctrl+Shift+R).

---

## References
- Previous handoff: `HANDOFF-2026-06-02.md`
- Driving meeting (T3 direction): `../01 Meetings/2026-06-02 Jason & Nadav — Weekly Sync.md`
- Design system: `../07 Systems/TradeFundrr Landing Page Design System.md` (§15 added today; §14 T3 marked implemented)
- Homepage design + messaging: `../04 Content/HOMEPAGE-DESIGN-MESSAGING.md` (current copy; the `main-site/` copy had drifted and was given a pointer note)
- Christian's design notes (referenced in the futures review): `../01 Meetings/2026-05-28 - Jason × Christian.md`, `2026-05-31 - … Web Edits Review.md`, `2026-06-01 - … Website Edits Review.md`; brief `../03 Offers/2026-05-28 Stocks & Options Inner Pages Brief — Christian.md`
- Mockups (gitignored scratch): `t3-foundation-mockup.html`, `payout-rain-mockup.html`, `stocks-hero-tilt-mockup.html`, `stocks-hero-assemble-mockup.html`, `stocks-hero-assemble-sidebyside-mockup.html`

*Last updated: 2026-06-03 — T3 foundation-stack sitewide; futures proof-reorder + compliance/copy + structural-difference card; Geist Mono standardization; payout-rain "Paid. Every Friday." (futures + homepage); futures critical review delivered; stocks hero scroll-animation mockups (not live).*
