# Marketing Site Session Handoff — 2026-06-02

**For:** the next Claude instance, or Jason on a future revisit
**Repo:** `C:\Users\Trader\OneDrive\Desktop\TradeFundrr Brain\main-site\`
**Deploys to:** https://tradefundrr-main-site.vercel.app/ (NOT `tradefundrr.com`, still old WordPress)
**Previous handoff:** `HANDOFF-2026-06-01.md`

**Theme of the day:** finished the stocks storytelling page, polished the homepage, and did a heavy futures pass (button, Programs rework from the build sheet, payouts + reviews sections). Also rebuilt the About page, renamed the nav sitewide, and filed a Nadav weekly-sync meeting note.

> **Workspace rules still apply (carried from prior handoffs):** the agent CANNOT git push (no GitHub creds) — Jason pushes from his own machine. Do NOT read/write the big HTML files through bash/cat/python/sed (the OneDrive mount serves stale/truncated copies) — use the `Read`/`Write`/`Edit` file tools; `grep`/`git` via bash are fine. Bump `?v=` cache-busters only when editing `assets/css/*`; inline HTML/CSS deploys fresh. Mockups (`*-mockup.html`, etc.) are `.gitignore`d scratch in `main-site/` root.

---

## TL;DR — what shipped today

### Stocks page (`pages/stocks.html`) — storytelling page finished
1. **Zero Net Cost section** replaced the old equity-traders ("Funded trading grew up around futures… stock traders got the afterthought") section. Animated ledger: `−$1,499` evaluation fee (struck through) → `+$1,499` rebate → `+$800` (your 80% of a $1,000 payout request) → climax of two big numbers: **`$0` net cost** + **`+$2,299` total on first payout** (count-up), then a grunge **"ZERO NET COST"** stamp slams in (SVG-filter ink + shockwave ring). Compliance-threaded (fee rebated on first qualifying payout, "simulated", `$0`-only-on-pass hedge). New classes `znc-*`, `#znc-grunge` filter.
2. **Equity-scale section** added after Zero Net Cost — *"You've proven you can trade. You haven't proven you can scale."* + the **"20% problem" box** ($25K→$5,000 vs $100K→$20,000, count-up) + *"Until TradeFundrr."* bridge. Trimmed per dedup: dropped the scared-money and futures-afterthought blocks (covered by downside-capped / removed earlier). Classes `eqs-*`.
3. **How It Works reworked** to v2: *"Here's what happens when you join"* → Express/Growth two-ways-in (Express = $1,499 funded day one, NO eval; Growth = 2-stage eval) → three step cards → **"Earned, by invitation"** T3 close (no "stay in sim forever"). Net-cost line dropped (Zero Net Cost owns it).
4. **Capital-gap graphic** (`capital-gap-graphic.html`): removed em-dash in the diamonds pull-quote; **loaded the real site fonts** (was falling back to system — the iframe loaded none); converted ALL figures (column amounts, the 100%/5% returns, the $5,000 results, the three stat-card numbers) from monospace/Playfair to **Inter 900 big-number style** in site teal/crimson/gold; fixed the profit-split line wrapping (`text-wrap:balance`, removed forced `<br>`s).
5. **Animations gated to scroll-into-view** — removed the 2.6s fallback timers on the Zero Net Cost + equity-scale sections (they were firing the count-up/stamp before scroll).
6. **Removed the leftover `MOCKUP` dev banner** above the footer.
7. **Audited the back half** (Big 5, FAQ, business model, T3, reviews, platforms, who-it's-for, separators, why-now, final CTA) against v2 → all aligned with or improved past v2; no rework needed. The page consistently fixes v2's compliance weak spots (em-dashes, "simulated", T3 registrations scoped to T3 Trading Group, no unconditional refund promises).

### Homepage (`index.html`)
8. **How It Works section:** confetti now sprays **`$` glyphs** (was circles/squares) in the same palette; **removed the time-pills** from all four step callouts; **Step 4 → "Scale to $20M in Live Buying Power"** (with by-invitation/top-performer hedge); Step 3 forced line break ("Prove Consistency." / "Earn Payouts.").
9. **Get Funded routing:** the nav, hero, and "Stop Risking Your Savings" CTAs now route to **`#markets`** (the 4-market chooser) instead of `pricing.html` — since there are 4 asset classes, "Get Funded" sends visitors to pick a market first. **Crypto market-card "Explore" fixed** (was pointing at a `slimehide.github.io` test URL) → `pages/pricing.html`.
10. **Numbers section ("Real Traders. Real Payouts…"):** added a **shaded frosted panel** behind the text so it reads over the falling payout cards (`.numbers__panel`, `rgba(8,12,22,.62)` + blur). Widened to the full 1240px container with slim padding so each 48px line stays on one row.

### Futures page (`pages/futures.html`) — heavy pass
11. **Nav "Get Funded" button → blue**, matching the homepage button **exactly** (full pill, 200px radius, 18px, glow) but in blue (`#00AAFF → #0077cc`). Root cause: `.btn--primary` is hardcoded teal in `main.css` and the futures `.btn`/nav overrides shrank it; fixed with a scoped `.navbar__cta.btn--primary` override that restores the homepage geometry in blue. Also **anchored to `#programs`**.
12. **Programs section reworked** from the build sheet (`futures-programs-build-sheet 1.html`): **Funded / Pro Trader Funding tabs** at the top (Pro moved out of the cards into its own tab/panel linking to `/pages/pro-funding.html`); a **50K / 100K size toggle** that swaps every figure; the **$19 Promo** surfaced as its own gold card (50K-only — 50K shows three boxes Promo·Growth·Express, 100K shows two); a **shared-rules chip strip**; and an **auto-open full-spec table** (Promo 50K · Growth 50K/100K · Express 50K/100K). Reuses the existing `.pcard`/`.pc-*` classes + a `pr-rework-css` style block. **80/20 confirmed for futures** by Jason (build sheet had flagged it).
13. **Deleted the How It Works section** — redundant/patronizing for the hyper-aware futures audience (they know the eval→funded→payout cycle; it was also covered by the mechanics spine + the reworked Programs). Kept the "most of it ends in sim" them-vs-us section (the strongest hyper-aware beat). Flow is now: hero → "already funded elsewhere" lead → mechanics spine → them-vs-us → **Programs** → Big 5 → switcher → FAQ → business model → T3 → **Payouts** → Reviews → platforms → who-it's-for → separators → CTAs.
14. **Payouts/falling-cards section** ported from the homepage Numbers section, **blue**: cards hue-rotated teal→blue (`hue-rotate(40deg)`), the **blue TradeFundrr logo**, **no fade** (all four lines full white, solid-blue accent words), shaded panel, **static green card background layers removed** (`.numbers` bg + `.bg` — those were the "frozen green cards"; only the falling blue cards remain). Placed **before Reviews**. Section `#payouts`.
15. **Reviews section** replaced with the **homepage `.featured-reviews` design**, recolored blue. Futures loads `mobile-fixes.css` so the design's available; added a `rev-blue-css` block overriding the six teal accents (summary stars, summary-brand hover, label, review stars, cta link/border, head accent gradient, hover border/glow) to `#00AAFF`. 9-card grid, Trustpilot summary, "read all" link, disclaimer.

### About page (`pages/about.html`) — rebuilt earlier in the day
16. Rebuilt from the Why-Us blueprint PDF: **trust hero** ("We're not here to just give you funding. We're here to earn your trust."), **credibility ticker** (ported from homepage), **founders section** ("One face. Real infrastructure behind it." — the missing piece), **"The Ultimate Pathway"** (replaced the homepage-ported Two Paths), **Transparency / Simplicity / Abundance** (replaced "Four commitments"), **4-market router** (replaced the comparison table — table now hidden, lives on why-us.html), and **de-duped the legal** (removed a duplicate disclosure section + orphan style tag). Compliance-threaded throughout.

### Site-wide / vault
17. **Nav "About" → "Why Us"** sitewide (16 files: `index.html` + 15 `pages/*.html`), keeping the link target. Note: the nav "Why Us" points at `about.html`; the separate receipts-style `why-us.html` is now orphaned from the nav (decide later: retire / merge / repoint).
18. **Filed the Jason × Nadav weekly sync** (`01 Meetings/2026-06-02 Jason & Nadav — Weekly Sync.md`) + 6 task files in `10 Tasks/` + touchpoints on `Jason Love.md` / `Nadav Sapeika.md`. Headline item: **Nadav's T3 Trading Group positioning feedback** (see Outstanding #1).

---

## ⚠ Outstanding Items (do these next)

1. **T3 positioning restructure (from the Nadav sync).** Nadav questioned leading with T3 Trading Group ("we're not working with them") and recommended showing **T3 Global built ON the T3 Trading Group foundation** — an arrow or "foundation" graphic, with framing "T3 Global is built on the same technology, platforms, compliance, ops, and trade desk as T3 Trading Group; it just isn't where you graduate to." Applies to the About + futures (and stocks) T3 sections + the design-system T3 rules. Task: `10 Tasks/2026-06-02 T3 positioning graphic restructure.md`.
2. **Futures CTA routing pass (task #12/#13).** The futures hero "Get funded for $129" already hits `#programs`; sweep the **who-it's-for** and **final-CTA** buttons to `#programs` too. Then do the same on **stocks / options / pricing** product pages (many CTAs are placeholder `href="#"`).
3. **Dead code:** orphaned `.bu-*` CSS + blow-up `<script>` in `futures.html` (from the prior session) and now-orphaned `.eq-*` (stocks), `.hiw-*` (futures, after the HIW delete), and `.rev`/`.rev-card` (futures, after the reviews swap) CSS. Harmless; strip on a cleanup pass.
4. **Options page = stocks page, text swapped.** Stocks is now locked; port the Zero Net Cost + equity-scale + reworked HIW + the storytelling structure to `pages/options.html`.
5. **Design-system "No PDT references" rule is stale.** The stocks **Why-Now** section accurately uses the **June 4, 2026 PDT / $25K-minimum rule change** (FINRA effective 2026-06-04, broker phase-in to Oct 20, 2027 — verified via FINRA Notice 26-10 + SEC). Updated the design-system note today so a future session doesn't strip the (correct) section. (See Design System §14.)
6. **Pre-existing em-dashes** in `stocks.html` (a mobile CSS label `.b5-val::before`, the time-tax study citation, and a "Rebate eligible — see terms" line) — not introduced today; clean on a pass.
7. **Crypto** still has no dedicated page; Crypto routes to `pricing.html`. The futures Pro tab links to `/pages/pro-funding.html` (the real 50KB page; the tiny `futures-pro-funding.html` is a stub).

---

## Push commands (Jason runs these)

All of today's edits are uncommitted until pushed. Files touched today: `index.html`, `pages/stocks.html`, `pages/about.html`, `pages/futures.html`, `capital-gap-graphic.html`, and the 15 other `pages/*.html` for the nav rename. A catch-all:

```
cd "C:\Users\Trader\OneDrive\Desktop\TradeFundrr Brain\main-site"
git add index.html capital-gap-graphic.html pages/*.html
git commit -m "2026-06-02: stocks Zero-Net-Cost/equity-scale/HIW; homepage confetti+panel+routing; futures button+Programs+payouts+reviews; About rebuild; nav About->Why Us"
git push origin main
```

After Vercel rebuilds (~30–60s), hard-refresh (Ctrl+Shift+R).

---

## References
- Previous handoff: `HANDOFF-2026-06-01.md`
- Driving meeting: `../01 Meetings/2026-06-02 Jason & Nadav — Weekly Sync.md`
- Build sheet: `futures-programs-build-sheet 1.html` (uploads) — the futures program SKUs
- Why-Us blueprint: the "Why Us Page - 2026.pdf" (uploads) — drove the About rebuild
- Design system: `../07 Systems/TradeFundrr Landing Page Design System.md` (§14 added today)
- Homepage design + messaging: `../04 Content/HOMEPAGE-DESIGN-MESSAGING.md` (updated today)
- Mockups (gitignored scratch): `zero-net-cost-mockup.html`, `equity-scale-mockup.html`, `how-it-works-mockup.html`, `hiw-full-mockup.html`, `futures-programs-mockup.html`, `futures-payouts-mockup.html`

*Last updated: 2026-06-02 — stocks storytelling finished (Zero Net Cost, equity-scale, HIW rework, capital-gap graphic typography); homepage ($ confetti, Numbers panel, Get Funded → #markets); futures (blue button, Programs rework from build sheet, deleted HIW, blue payouts + reviews); About rebuild; nav About → Why Us sitewide; Nadav sync filed.*
