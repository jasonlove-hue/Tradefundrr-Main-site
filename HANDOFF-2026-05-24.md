# Marketing Site Session Handoff — 2026-05-24

**For:** the next Claude instance picking up this work, or Jason on a future revisit
**Repo:** `C:\Users\Trader\OneDrive\Desktop\TradeFundrr Brain\main-site\`
**Deploys to:** https://tradefundrr-main-site.vercel.app/
**Previous handoff:** see `2026-05-23 Marketing Site Session - Handoff.md` (yesterday's session)

---

## TL;DR

Major session focused on positioning and Hormozi-frame messaging revisions across the homepage. Shipped: new hero headline ("Risk your edge. Not your savings."), founder section (Christian Bose), Journey/timeline section with animation, audience-avatar section (4 segments), simplified pricing cards, and a payout guarantee block. Plus a sitewide em-dash sweep, PDT-repeal cleanup, T3 partnership rewrite, adaptive favicon, and a How It Works section overhaul.

**The guarantee block is CONDITIONAL — see "Guarantee Removal Instructions" below if it needs to come out.**

---

## ⚠ Guarantee Removal Instructions ⚠

The "Hit your first payout. Get your eval fee back." guarantee block was shipped at Jason's direction with the explicit note that **it may need to be removed** depending on business viability. The implementation is wrapped in clean START/END markers throughout so removal is a clean three-step operation.

### To remove the entire guarantee:

**Step 1 — Remove the primary banner from `index.html`**
- Open `index.html`
- Find the comment marker `<!-- GUARANTEE_BANNER_START -->` (inside `<section class="plans">`, between the section header and the pricing grid)
- Delete everything from that marker through `<!-- GUARANTEE_BANNER_END -->` (inclusive)

**Step 2 — Remove the per-card badges from `index.html`**
- Inside each of the three pricing cards (Options, Stocks, Futures), find this pattern:
  ```html
  <!-- GUARANTEE_BADGE_START --><span class="stack-card__guarantee">Refunded with first payout</span><!-- GUARANTEE_BADGE_END -->
  ```
- Delete each occurrence (three total, one per card)
- A quick grep: `grep -n "GUARANTEE_BADGE" index.html` will surface all three lines

**Step 3 — Remove the CSS block from `mobile-fixes.css`**
- Open `assets/css/mobile-fixes.css`
- Find `/* === GUARANTEE START === */`
- Delete everything from that marker through `/* === GUARANTEE END === */` (inclusive)

**Step 4 — Bump the cache-buster on `mobile-fixes.css`**
- In `index.html`, find the line:
  ```html
  <link rel="stylesheet" href="assets/css/mobile-fixes.css?v=YYYYMMDDHHMM" ...>
  ```
- Update the `?v=` query string to a fresh timestamp so browsers re-fetch the updated CSS

**Step 5 — Commit and push**
```powershell
git add index.html assets/css/mobile-fixes.css
git commit -m "Remove payout guarantee block + per-card badges"
git push origin main
```

Total removal time: under 5 minutes if done carefully. No other section depends on the guarantee, so nothing else breaks.

---

## Business Decisions Pending on the Guarantee

If Jason wants to KEEP the guarantee long-term, three business mechanics still need to be locked in. They're not blocking the live experience but will become live questions when traders start earning payouts:

1. **Refund delivery mechanic** — Is the eval fee refund (a) ADDED to the first qualifying payout (so trader gets payout + $899 in a single wire), or (b) refunded separately to the original payment method (card/bank)?
2. **Qualifying payout definition** — Does any approved payout request trigger the refund, or is there a minimum threshold (e.g., the payout must be ≥ $500, or must exceed the eval fee)?
3. **Coverage scope** — Currently the banner says *"available on all instant funding plans"*, which includes the $179 Futures plan. If Jason wants Futures excluded (since the fee is much lower), update the banner copy to *"available on $899 instant funding plans"* and potentially remove the badge from the Futures card.

---

## What Was Shipped Today

### 1. Hero overhaul

- Headline: `"Risk your edge. Not your savings."` (pain-led, replaces feature-led `"Fund Your Stocks, Options, Futures, Or Crypto Trading With Up To $100k"`)
- Accent span on `"Not your savings."` (teal)
- Headline font-size bumped to 96px desktop (from 70px base in `main.css`); letter-spacing tightened to -3.8px
- Sub-headline rewritten as two declarative sentences on separate lines (forced via `<br>`):
  > *"Trade a $100k simulated funding account with transparent rules. / Top performers graduate to live capital through T3 Global."*
- `.hero__text` max-width expanded from 600px to 720px to accommodate larger H1
- Eyebrow unchanged ("Powered by Institutional-grade trading technology")

### 2. Founder section (Christian Bose)

- New `<section class="founders">` inserted between the T3 partnership section and the Human Support section
- Wrapped in `<!-- FOUNDERS_SECTION_START -->` / `<!-- FOUNDERS_SECTION_END -->` markers
- Portrait image: `assets/images/christian_headshot.png` (added by Jason; tracked in git)
- Layout: bio on left, 420px portrait on right (used CSS `order` to flip without changing HTML)
- Offset teal frame behind portrait extends down-left
- Headline: *"Run by an operator from the institutional side."* (accent on "institutional side.")
- CSS lives in `mobile-fixes.css` under `/* === FOUNDERS CSS START === */`

### 3. Journey / Timeline section

- New `<section class="journey">` inserted between the Human Support section and the mid-page CTA section
- Wrapped in `<!-- JOURNEY_SECTION_START -->` / `<!-- JOURNEY_SECTION_END -->` markers
- Four cards on a timeline rail: Week 1 / Weekly / Months 1+ / Earned
- Cards: "Pass the Eval." / "Weekly Payouts. On Performance." / "Earn Live Capital." / "Audition for the T3 Global Trading Desk."
- Cycle animation matching HIW's 2.75s cadence; pauses on hover
- Inline `<script>` at the end of the section handles the cycle + active-state toggling
- CSS lives in `mobile-fixes.css` under `/* === JOURNEY / TIMELINE SECTION === */`

### 4. Avatar section (4 segments)

- New `<section class="avatar">` inserted between the credibility ticker and the Two Paths section
- Wrapped in `<!-- AVATAR_SECTION_START -->` / `<!-- AVATAR_SECTION_END -->` markers
- Headline: *"The edge isn't the problem. Capital is."* (accent on "Capital is.")
- Four segment cards (Stocks / Options / Futures / Crypto), each anchored on `$5K of personal capital` and ending with a parallel "X works. Y doesn't." kicker
- Closer paragraph at the bottom carries the documented-pathway moat phrase
- Grid: 4-up at ≥1200px, 2x2 at 901-1199px, 1-up at ≤600px
- Container max-width: 1550px (wider than other sections so 4-up isn't cramped)
- Subtle bg shift (`#0a101e` vs surrounding `#060914`) + teal hairline divider at section bottom for visual separation
- CSS lives in `mobile-fixes.css` under `/* === AVATAR SECTION === */`

### 5. How It Works section overhaul

- Headline changed to *"From Evaluation to Funded Trader."* (accent on "Funded Trader.")
- Subheader: *"Four steps. Earned by performance. Only the most disciplined reach the final stage."*
- Step 4 callout title changed from `"Move to a Live Funded Account"` to `"Earn Live Capital."`
- Step 4 description updated to mention the audition path
- Header bottom-margin bumped from 36px to 90px so the chart doesn't crowd the headline
- HIW chart animation timing: `cycleMs = 2750` (2.75s per step), confetti on step 4 (desktop only), retract animation back to step 1 (visible draw-back, not fade-out)
- Pulse animation on active step: single-shot ~1.2s (was infinite breathing)

### 6. Pricing cards simplification (multiple iterations)

Settled on this final structure after several attempts:

- **Identity** (eyebrow + title + subtitle)
- **Hero claim** in a horizontal-border block:
  - Options: *"$25K options buying power. No personal capital tied up."*
  - Stocks: *"$100K buying power via 4x leverage. No personal $25K at risk."*
  - Futures: *"$100K notional margin. No personal capital tied up."*
- **Flat 7-bullet feature list** (no tier headers, no per-line dollars, no value stacking)
- **Centered price block**
- **Guarantee mini-badge** under the price (see Guarantee Removal Instructions if removed)
- **CTA button**

### 7. T3 partnership section rewrite

- Lead paragraph rewritten with regulatory chops attributed to "T3 group of companies":
  > *"TradeFundrr is partnered with T3 Global, an institutional trading firm operating within the T3 group of companies. Member firms include a regulated SEC broker-dealer and FINRA/SIPC member. Most funded-trader programs can't say that."*
- T3 logo image swapped from `broker-deal.svg` to `t3_global_logo.png` (added by Jason)
- Logo expanded to fill full card width (was capped at 220px)
- Logo inverted to white via CSS `filter: brightness(0) invert(1)` for legibility on dark bg
- `.institutional__title .accent` overridden to teal (was yellow `#fc0`)

### 8. Path 02 (Two Paths section)

- Headline changed from `"T3 Global Live Capital"` to `"Graduate to Real Money"`
- Body extended with audition narrative: *"The very best audition for a seat on T3 Global's trading desks."*
- "Most prop firms run sim only and call it live." line-break forced after "live." via `<br>`

### 9. Testimonials section header

- Tagline changed from *"Real names, real payout amounts, real accounts. No anonymous testimonials..."* to *"Real names. Real reviews. No anonymous testimonials."*
- Reason: the Trustpilot widget delivers reviews but not specific payout amounts, so removed the "payout amounts" claim

### 10. Mid-page CTA rewrite

- Headline: *"Stop Watching. Start Trading."* (was *"Your Edge is Real. Your Funding Should Be Too."* — too redundant with hero)
- Sub: *"Start with a $100k simulated funding account. Hit performance targets, earn weekly payouts. Top performers graduate to live capital through T3 Global."*

### 11. Numbers section: TradeFundrr logo

- "TradeFundrr is also" text changed to: logo image + "is also" text
- Logo height 32px desktop, 26px mobile

### 12. Sitewide em-dash sweep

- All em-dashes (`&mdash;`, `—`, `&#8212;`, `&#x2014;`) replaced with commas across all 16 production HTML pages
- Spacing artifacts cleaned up after the sweep
- Caveat: en-dashes (`&ndash;`, `–`) were NOT swept and remain in place (e.g., for numeric ranges)

### 13. PDT cleanup sweep

- PDT (pattern day trader) rule was repealed; all references stripped
- Updated: testimonial quote in 6 files, meta descriptions on stocks.html + options.html, copy in stocks.html
- Note: `assets/images/pdt-icon.svg` filename remains in the repo and is referenced once in `pages/stocks.html` (not user-visible, just an image-file name)

### 14. Accent color standardization

- All H2 `.accent` spans across the site forced to teal (`#22d3a8`) via override in `mobile-fixes.css`
- Was: yellow (`#fc0` / `#ffcc00`) on multiple section accents
- Exception: Blackbook section retains its gold accent (`#ffc107`) because it matches the book image

### 15. Favicon

- Single dark favicon (`tf-icon-dark.png`) used sitewide
- Earlier experiments with adaptive (light icon for dark mode via JS) were reverted because Chromium browsers can have OS dark mode + light browser chrome (the JS-based swap then renders the wrong icon)
- File `tf-icon-light.png` is in the repo but unreferenced; can be deleted in a future cleanup

### 16. Section visual treatment

- Avatar section: subtle bg shift + bottom teal hairline divider
- Plans section: solid `#060914` background with two radial teal glows (replaced the underlying grid pattern that was bleeding through, kept the atmospheric glow)

---

## Files Modified Today

- `index.html` — major sections added (founders, journey, avatar, guarantee), hero rewrite, HIW overhaul, all pricing cards rebuilt, mid-CTA rewrite, T3 section rewrite, hundreds of smaller copy edits
- `assets/css/mobile-fixes.css` — extensive CSS additions for new sections + accent overrides + plans-section bg + guarantee block
- All 15 production HTML pages — em-dash sweep, PDT cleanup, favicon link tags added
- `pages/why-us.html` — spacing fix on bullet list items
- `pages/stocks.html` — additional PDT-related copy fixes
- New assets committed: `assets/images/christian_headshot.png`, `assets/images/t3_global_logo.png`, `assets/images/tf-icon-dark.png`, `assets/images/tf-icon-light.png`

---

## Untracked Preview / Mockup Files

These files live in the repo root but are NOT deployed. They were built as visual previews during the session and can be deleted at any time (or kept as design history).

- `founder-preview.html`
- `t3-section-preview.html`
- `journey-preview.html`
- `journey-questions.html`
- `pricing-stack-preview.html` (now superseded — see `pricing-stack-v3-preview.html`)
- `pricing-stack-v3-preview.html`
- `avatar-preview.html`
- `hormozi-questions.html`
- `guarantee-preview.html`

---

## Open Items From Hormozi Audit (Not Yet Shipped)

1. **Urgency / scarcity mechanic** — Still on the audit list. No founder-pricing window, no cohort cap, no deadline mechanism anywhere on the page. Requires a real business commitment (e.g., "Q3 cohort, 50 seats" or "Founder pricing through [date]"). Untouched this session.

2. **Concrete proof numbers** — Jason previously asked to skip adding a dedicated payouts proof stat. Worth revisiting if/when there's defensible aggregate data to publish (e.g., "X traders funded since 2024", "$X in payouts processed").

3. **Pre-existing pro-funding page truncation** — Three pages (`futures-pro-funding.html`, `options-pro-funding.html`, `stocks-pro-funding.html`) still end mid-paragraph in the CFTC disclosure with no `</html>` closing tag. Pre-existing from the May 23 OneDrive corruption per yesterday's handoff. Not addressed this session. Restoration approach: pull a clean version from git history (`git show <hash>:pages/<file>`) or rebuild from a sibling page.

---

## Conversation / Behavior Notes for Next Instance

Carry-overs from yesterday's handoff that still apply:

- **OneDrive corruption is still a recurring risk.** Repo lives in `C:\Users\Trader\OneDrive\Desktop\...`. Multiple files got truncated during this session by OneDrive racing with file writes. When in doubt, use the Read tool (host filesystem) over bash (sandbox view, which is sometimes stale). For large multi-line writes, prefer many small `Edit` calls over a single `Write` of the full file.
- **Strongest recommendation from yesterday still stands**: move the repo out of OneDrive to `C:\dev\tradefundrr\` (or similar non-synced path). Every sed/large-write operation is a coin flip for truncation as long as it lives in OneDrive.

New observations this session:

- **Jason is decisive and action-oriented.** Will say "yes do it" or "go with X" rather than asking for elaborate previews. Match that pace — when he picks an option, execute rather than asking for confirmation.
- **Jason responds well to specific recommendations with brief rationale.** Don't list 5 options without picking one. Recommend, explain in 1-2 sentences, give him the choice.
- **Honest pushback on AI suggestions is welcome.** Jason fed several AI-generated alternative copy suggestions during the session and explicitly asked "do you agree?" — he wanted reasoned disagreement when warranted, not blind acceptance.
- **Preview HTML files in main-site root are the established pattern** for visual mockups. Jason opens them locally, reviews, and signs off before porting to production. Use this pattern for any new section that needs visual review.
- **Avoid em-dashes in any new written text per sitewide policy.** Use commas or periods. En-dashes are fine (e.g., for numeric ranges like "1–2 months").

---

## Important File Locations / Conventions

**Section idempotency markers in `index.html`:**

- `<!-- TRUSTPILOT_HEAD_START/END -->`
- `<!-- TRUSTPILOT_TUNE_CSS_START/END -->`
- `<!-- TRUSTPILOT_WIDGET_START/END -->`
- `<!-- PORTAL_SPARKLES_CSS_START/END -->`
- `<!-- PAYOUT_CARDS_START/END -->`
- `<!-- PRO_SUB_STOCKS_START/END -->` (same for OPTIONS and FUTURES)
- `<!-- FONTS_BLOCK_START/END -->`
- `<!-- INLINE_FONTS_START/END -->`
- `<!-- REMOVE_FLOOR_CSS_START/END -->`
- `<!-- LEGAL_DISCLOSURE_START/END -->`
- `<!-- BLACKBOOK_SECTION_START/END -->`
- `<!-- FOUNDERS_SECTION_START/END -->` (added today)
- `<!-- JOURNEY_SECTION_START/END -->` (added today)
- `<!-- AVATAR_SECTION_START/END -->` (added today)
- `<!-- GUARANTEE_BANNER_START/END -->` (added today, may be removed — see top)
- `<!-- GUARANTEE_BADGE_START/END -->` × 3 cards (added today, may be removed)

**CSS block markers in `mobile-fixes.css`:**

- `/* === BLACKBOOK CSS START/END === */`
- `/* === FOUNDERS CSS START/END === */`
- `/* === T3 PARTNERSHIP SECTION OVERRIDES START/END === */`
- `/* === ACCENT COLOR CONSISTENCY OVERRIDES START/END === */`
- `/* === JOURNEY / TIMELINE SECTION START/END === */`
- `/* === PRICING STACK CARDS START/END === */`
- `/* === AVATAR SECTION START/END === */`
- `/* === NUMBERS SECTION LEAD START/END === */`
- `/* === PLANS SECTION BACKGROUND OVERRIDE START/END === */`
- `/* === GUARANTEE START/END === */` (added today, may be removed)

**Cache-buster convention:** `?v=YYYYMMDDHHMM` query param on every CSS / JS / image link. Bump whenever the underlying file changes so Vercel + browsers fetch fresh. Latest `mobile-fixes.css` version at end-of-session: `?v=202605241850`.

---

# CONTINUATION SESSION — 2026-05-24 (evening) / pickup 2026-05-25

Picking up from the original handoff above. This session ran a deep designer + conversion audit on the live site and shipped roughly two dozen more changes. The major themes were: building out the loss-aversion arc, replacing the Trustpilot iframe with native review cards, swapping the final-CTA stock image for a Christian Bose quote card, completely rebuilding the Human Support section around a real team member (Dawson L.), and a sitewide messaging cleanup audit.

Latest `mobile-fixes.css` cache-buster: **`?v=202605252700`**.

---

## What Was Shipped This Continuation

### 17. Two-paths section: THEM/US contrast rows

- Added a `.path-card__contrast` block inside both Path 01 and Path 02 cards
- Each card now has two rows after the bullet list: a strikethrough "THEM" claim + a teal "US" counter-claim
- Path 01: *"'Live capital' that's still a sim payout"* (them) vs *"Sim payouts labeled sim. No mislabel."* (us)
- Path 02: *"No upgrade path. Sim is the ceiling."* (them) vs *"Earn live capital through T3 Global routing"* (us)
- Headline 2-line lock: forced break + nowrap so "Most prop firms run sim only and call it live." renders on a single line at desktop widths
- Eyebrows fixed: `.two-paths__eyebrow` and `.path-card--start .path-card__eyebrow` were hardcoded to `#fb923c` (orange) in inline `<style>` in `index.html` lines 911 / 933 — both changed to `#22d3a8` (teal)
- HTML markers: `<!-- PATH_CONTRAST_01_START/END -->` and `<!-- PATH_CONTRAST_02_START/END -->`
- CSS markers: `/* === PATH CONTRAST ROWS START/END === */` and `/* === TWO-PATHS TITLE TWO-LINE LOCK START/END === */`

### 18. Journey section refinements

- Headline rewritten: *"From your first evaluation to a seat at T3 Global."* (was *"The Path. In Real Days."*) with explicit `<br>` + nowrap on "a seat at T3 Global." to prevent widow
- Sub rewritten: *"A documented path. Not a marketing promise."* (was *"Specific days. Specific milestones..."*)
- Card 4 description: removed *"regulated broker-dealer's seat"* claim (compliance — T3 Global is the institutional prop firm, not the SEC broker-dealer; that's T3 Trading Group, a separate entity under T3 Companies)
- "eval" → "evaluation" globally in this section

### 19. Loss-aversion section (NEW — biggest addition)

- New `<section class="cost-of-waiting">` inserted between Journey end and Mid-CTA
- Wrapped in `<!-- LOSS_AVERSION_START/END -->` markers
- **Three-stage layout** ($5K → $100K → $1M+) via the `.cost-of-waiting--three-stage` modifier class
- Headline: *"Your edge has one ceiling. We move it. Twice."*
- Cards:
  1. **Trading Your Own Money** — $5,000 / *your ceiling* / *"Every loss is your money. Every win is capped by your account size."*
  2. **Funded Account** — $100,000 / *simulated capital* / *"Simulated funded account. Keep up to 80% of what you make. No personal risk."*
  3. **T3 Global Desk** — $1M+* / *desk capital* / *"Top performers earn a seat. Institutional infrastructure. Real fills."* — with "Earned" badge
- Compliance disclaimer on the $1M+ card: *"\*Reserved for top-performing traders. Subject to T3 Global review and audition. Not all traders qualify."* (italic 10.5px at 45% white, with thin separator above)
- Asterisk treatment: small teal superscript next to "$1M+"
- Closer: *"Start at $100K. Earn your way to $1M+."* (replaced earlier candidates "You don't have to be the ceiling." and "The edge is the same. The account is what's small.")
- Arrows between cards via `.cost-of-waiting__arrow` (rotates 90° on mobile when grid stacks)
- CSS markers: `/* === LOSS AVERSION (COST OF WAITING) START/END === */`
- The progression mirrors what the numbers section above promises ($1M+ at T3 desk), eliminating the earlier internal inconsistency

### 20. Mid-CTA + final CTA: hero-mirror copy

- Mid-CTA sub copy rewritten from generic features to hero-callback: *"Your edge isn't the problem. Your small account is. Trade $100K of simulated capital. Keep up to 80% of what you make. Pass once, earn back your eval fee."*
- "$5K" → "small account" (more general, also matches avatar lead-in)
- 90% → 80% profit split (compliance / accuracy fix — Options Express is 80%, sitewide stays consistent)
- Button label: "Get Funded Today" → **"Stop Risking Your Savings"** (Option A from the hero-mirror set, closes the loop on "Risk your edge. Not your savings.")
- Same button label applied to the final CTA at the bottom of the page

### 21. Final CTA: Christian Bose quote card replaces stock trader image

- Stock "stress→success" trader composite image (`assets/images/hero-image.webp`) DROPPED from the Get Started section
- Replaced with `<div class="founder-quote-card">` — same visual treatment as the founders portrait section
- Christian's portrait at 4:5 rectangle, dark gradient backdrop, subtle border, drop shadow
- Quote: *"Your edge got you here. Our job is to make sure your account size doesn't stop you."* (curly quotes, second half in teal accent)
- Attribution: **Christian Bose** / CO-FOUNDER · TRADEFUNDRR (thin divider above)
- HTML markers: `<!-- FOUNDER_QUOTE_CARD_START/END -->`
- CSS markers: `/* === FOUNDER QUOTE CARD START/END === */`

### 22. FAQ section: "Which means:" closers on all 24 entries

- Every FAQ answer across all 4 tabs (Trust & Transparency, Trading Experience, Funding & Payouts, Getting Started) now ends with a `<strong class="faq__means">Which means: ...</strong>` clause
- Visual: teal-left-border callout block with teal-tinted background, bold 14px text — clearly differentiated from the FAQ body
- Each clause reframes back to a benefit listed elsewhere on the page (e.g., "weekly payout windows, paid direct to your Rise account," "the cost of failure is the eval fee, not your savings," "a Monday signup can have you trade-ready the same week")
- CSS markers: `/* === FAQ WHICH-MEANS START/END === */`

### 23. Featured Trustpilot reviews (REPLACED the iframe)

- Removed the Trustpilot iframe widget entirely (38KB of JS + iframe + Trustpilot tracking gone)
- Built **9 verbatim 5-star reviews** in a 3×3 grid as native HTML cards
- Reviews pulled live from Trustpilot via Chrome MCP, filtered for 5-star + payout-related text
- Reviewers (all verbatim, never altered):
  1. **Chad Tillbrook** (US, Jan 12, 2026) — *"So glad I took the chance..."* (skeptic→believer narrative)
  2. **Hassan** (SA, Jan 9, 2026) — *"My Tradefundrr experience"* (6 trading days)
  3. **Parker B.** (US, Dec 23, 2025) — *"One of the best prop firms I've worked with"*
  4. **MOHAMED EL-BRAHMI** (FR, Apr 11, 2026) — *"I received my payout today"* (3rd payout)
  5. **Kajal Prakash** (US, Apr 4, 2026) — *"A very good experience with TradeFundrr"*
  6. **Eric** (US, Apr 30, 2026) — *"TradeFundrr Review - after several months"* (compared 5 firms, ranked #1)
  7. **Joyce** (US, Nov 22, 2025) — *"Fast Payout"* (money in account in a few hours)
  8. **Caleb** (US, Oct 8, 2025) — *"Can't say enough about this program"* (Instant Funding, 7 days)
  9. **Lisa** (US, Oct 6, 2025) — *"I have been with Tradefundrr"* (long-haul, 10 months)
- Each card highlights one bolded teal pull-quote within the verbatim body
- Summary stat header above the grid: ★★★★★ **"4.2 / 5 across 66+ verified reviews on Trustpilot"** (matches the real Trustpilot data — corrected from earlier inflated 4.8/200+ placeholder)
- "Read all 66+ verified reviews on Trustpilot →" link below the grid (links to the live Trustpilot page)
- Card heights equalized via `grid-auto-rows: 1fr` so the 3×3 grid renders uniformly even when text length varies
- HTML markers: `<!-- FEATURED_REVIEWS_START/END -->`
- CSS markers: `/* === FEATURED REVIEWS START/END === */`
- **Dead code warning**: the original Trustpilot lazy-load `<script>` + `.trustpilot-widget` CSS are still in `<head>` (lines 300-368 area). Harmless — the script null-checks for `.trustpilot-widget` which doesn't exist anymore, the CSS targets nothing. Worth a cleanup commit later.

### 24. Numbers section: TF logo above the four lines

- Added the full TradeFundrr horizontal logo (`assets/images/logo.svg`) centered above the 4 stacked phrases ("Real Traders... Paid Direct... From Small Account... Scale to $1M+...")
- Sized 240px desktop / 180px mobile
- Soft teal drop-shadow glow so the logo reads against the falling payout cards background
- HTML markers: `<!-- NUMBERS_BRAND_MARK_START/END -->`
- CSS markers: `/* === NUMBERS BRAND MARK START/END === */`
- (Originally shipped with `tf-icon-dark.png` standalone icon, then upgraded to full logo per Jason)

### 25. Human Support section — COMPLETE REBUILD around Dawson L.

This was the most-iterated section of the session. Many passes through sizing, positioning, frame treatments, backgrounds, and finally a quote-card restructure to match Christian's founder card.

**Final state:**
- Stock photo (woman in headset, `human-support.webp` background image from main.css line 3014) — **REMOVED**
- Replaced with `.support-quote-card` — mirrors `.founder-quote-card` structure exactly
- Layout: quote card on LEFT, headline + body + Phone/Email Support cards on RIGHT
- Quote card contains:
  - **Portrait**: 220px wide, 4:5 aspect, rounded corners, dark gradient backdrop, subtle border + shadow (same treatment as Christian's portrait inside the founder quote card)
  - **Quote**: *"Real questions deserve real answers. That's the whole job."* (second half in teal accent)
  - **Attribution**: **Dawson L.** / TRADER SUCCESS SPECIALIST · TRADEFUNDRR (thin divider above)
- Outer card frame: teal-bordered rounded container with subtle teal-tinted gradient background
- Section background: `#0a1f28` (teal-dark) base + teal grid hairlines (1px at 10% opacity, 200×160px tile) + teal accent borders top/bottom at 18% opacity
- **Floating sparkles**: 32 white sparkle dots generated via inline JS, each with slow drift animation (45-95s cycle) + twinkle animation (3-8s cycle). Respects `prefers-reduced-motion`. Mirrors the hero's ambient sparkle treatment at smaller scale.
- HTML markers: `<!-- SUPPORT_DAWSON_START/END -->` (the quote card) and `<!-- SUPPORT_SPARKLES_START/END -->` (the sparkle generator script at end of section)
- CSS markers: `/* === SUPPORT QUOTE CARD START/END === */` (the new) and `/* === SUPPORT DAWSON START/END === */` (legacy styles kept for backwards compat)

**Image files in repo for this section:**
- `assets/images/dawson-landscape.png` — **the production image** (1401×1123 high-res, 1.25:1 aspect matching the original stock photo dimensions). Real photo, approved for use.
- `assets/images/Dawson landscape 820x657.png` — same file Jason uploaded with the original name; kept for reference
- `assets/images/support-team-member.png` — earlier square headshot version, no longer used
- `assets/images/support team member.png` — original upload with space in name, kept for reference
- `assets/images/Support Person call center.png` — **DO NOT USE.** AI-generated composite that Jason flagged as inauthentic. Kept in repo only as a reference for what NOT to ship.

**Compliance/honesty note on Dawson:**
- Dawson L. is a real Trader Success Specialist who handles **email tickets** (not phone). Photo approved for homepage use.
- The role label "Trader Success Specialist" was chosen as accurate without overclaiming phone responsibility.
- The quote ("Real questions deserve real answers") is Dawson speaking in voice, vetted by Jason.

**CSS overrides that fight main.css** (gotchas if revisiting):
- `.real-support__inner` has `max-width: 480px; margin-left: auto; width: 100%` at main.css line 7115 — must override with `!important` or grid collapses
- `.get-started__media img` has `height: auto` at main.css line 2700 — must override with `!important` for the founder quote card portrait to fill its 4:5 aspect container
- `.real-support` has `background: url("../images/human-support.webp")` at main.css line 3014 — overridden with `background: #0a1f28 !important` and `background-image: ...` for the new grid pattern

### 26. Founders section: Co-Founder consistency

- Title swapped briefly to "Founder" then reverted to **"Co-Founder"** sitewide
- All 4 references aligned: founders section alt text, founders section portrait badge, final CTA quote card alt text, final CTA quote card role attribution
- Zero remaining mismatches

### 27. Designer review fixes

After a full-page design audit, three issues from the review were shipped:
1. **Orange eyebrows on two-paths section** → teal (already covered above in #17)
2. **Equalized featured review card heights** (already covered above in #23)
3. **Replaced Human Support stock photo** with real Dawson L. (already covered above in #25)

Remaining audit items not yet shipped (next session candidates):
- Markets section uses stock photos for Stocks/Options/Futures/Crypto cards — proposed Option A (branded SVG visuals) but not yet built
- Numbers section TF logo can intermittently overlap with one of the falling payout cards (z-index issue)
- Trustpilot widget dead code in `<head>` still present (cleanup commit)
- FAQ tabs could use the "which means:" treatment time-tested before broader use

---

## Performance Snapshot (May 24, 7pm CDT — captured this session)

Lighthouse run on the live deployed page (`tradefundrr-main-site.vercel.app`):

| Metric | Mobile | Desktop |
|---|---|---|
| **Overall score** | **51** (RED) | **92** (GREEN) |
| First Contentful Paint | 3.5s | 0.7s |
| Largest Contentful Paint | 11.1s (RED) | 1.8s (slight orange) |
| Total Blocking Time | 0ms | 0ms |
| Cumulative Layout Shift | 0.282 (RED) | 0.003 (green) |
| Speed Index | 5.0s | 0.8s |

Mobile testing profile: Emulated Moto G Power + Slow 4G throttling + 4× CPU slowdown. Real users on faster networks will see much better numbers, but the underlying issues are real.

**Top Lighthouse "Insights" (both mobile + desktop):**
1. Render blocking requests — **560ms** estimated savings
2. Use efficient cache lifetimes — 5KB savings
3. Layout shift culprits (mobile only)

**Pending performance work — recommended priority order:**

1. **Preload the hero vault image** — `<link rel="preload" as="image" href="assets/images/vault-hero-nofloor.webp" fetchpriority="high">` in `<head>`. Biggest single LCP win, especially on mobile.
2. **Remove dead Trustpilot widget code** from `<head>`. Saves ~5KB + one HTTP request. The widget div is gone, the lazy-load script and CSS still reference `.trustpilot-widget`.
3. **Defer or async all non-critical JS.** jQuery already loads with `defer`, but `slick.min.js` loads for a testimonials slider we don't use anymore (replaced with the native 9-card grid). Drop slick entirely.
4. **Inline critical above-the-fold CSS** in `<head>`, lazy-load the rest. Biggest cumulative impact but requires a build-step or careful manual extraction.
5. **Audit all `<img>` tags for missing `width`/`height` attributes.** The CLS 0.282 on mobile is largely from images that don't reserve space before loading.

---

## Files Modified / Added This Continuation

**Modified:**
- `index.html` — many sections: two-paths contrast, journey copy, loss-aversion (new), mid-CTA copy + button, final CTA quote card (new), FAQ which-means (24 entries), featured reviews (new — replaces iframe), numbers section logo, human support rebuild (Dawson quote card + sparkles script)
- `assets/css/mobile-fixes.css` — extensive additions for every section above

**New asset files added to `assets/images/`:**
- `dawson-landscape.png` — the production Dawson photo
- `Dawson landscape 820x657.png` — original-name copy (kept for reference)
- `support-team-member.png` — earlier headshot variant (unused)
- `support team member.png` — original-name copy of the headshot (unused)
- `Support Person call center.png` — AI-generated, **DO NOT USE**, flagged by Jason

**Preview HTML files created (not deployed, kept in repo root):**
- `fix-1-two-paths-contrast-preview.html`
- `fix-2-journey-card4-preview.html`
- `fix-3-faq-which-means-preview.html`
- `fix-4-trustpilot-header-preview.html`
- `fix-5-loss-aversion-preview.html`
- `fix-6-final-cta-mirror-preview.html`
- `loss-aversion-option-a-preview.html`
- `loss-aversion-option-b-preview.html`
- `loss-aversion-option-c-preview.html`
- `support-team-preview.html`
- `dawson-quote-card-preview.html`

---

## Updated Section Markers Index

**HTML markers in `index.html` (added this continuation):**
- `<!-- PATH_CONTRAST_01_START/END -->` and `<!-- PATH_CONTRAST_02_START/END -->`
- `<!-- LOSS_AVERSION_START/END -->`
- `<!-- FEATURED_REVIEWS_START/END -->` (replaces the now-removed `<!-- TRUSTPILOT_WIDGET_START/END -->` block)
- `<!-- NUMBERS_BRAND_MARK_START/END -->`
- `<!-- FOUNDER_QUOTE_CARD_START/END -->` (inside the get-started/final CTA section, replaces the original stock image)
- `<!-- SUPPORT_DAWSON_START/END -->` (replaces the original `<img src="human-support.webp">`)
- `<!-- SUPPORT_SPARKLES_START/END -->` (sparkle generator script, end of `.real-support` section)

**CSS markers in `mobile-fixes.css` (added this continuation):**
- `/* === PATH CONTRAST ROWS START/END === */`
- `/* === TWO-PATHS TITLE TWO-LINE LOCK START/END === */`
- `/* === FAQ WHICH-MEANS START/END === */`
- `/* === LOSS AVERSION (COST OF WAITING) START/END === */`
- `/* === FEATURED REVIEWS START/END === */`
- `/* === NUMBERS BRAND MARK START/END === */`
- `/* === FOUNDER QUOTE CARD START/END === */`
- `/* === SUPPORT QUOTE CARD START/END === */` (the live Dawson treatment)
- `/* === SUPPORT DAWSON START/END === */` (legacy styles kept; not actively used)

---

## Open Items For Next Session

Carryovers from earlier handoff that are STILL pending:
1. **Urgency / scarcity mechanic** — still not addressed. Page has no cohort cap, no deadline, no founder pricing window
2. **Pro-funding pages truncation** — `futures-pro-funding.html`, `options-pro-funding.html`, `stocks-pro-funding.html` still end mid-paragraph
3. **Concrete proof numbers** — still pending business decision (X traders funded, $Y paid out)

New items added this session:
4. **Markets section stock photos** — Stocks/Options/Futures/Crypto cards still use stock photography. Proposed Option A (branded SVG candlestick/Greeks/ticker/BTC visuals) but not built. See conversation thread for details.
5. **Performance optimization** — preload hero image + remove dead Trustpilot JS + drop slick.min.js are the quickest wins. Critical CSS inlining is the biggest payoff but bigger lift.
6. **Numbers section logo z-index** — the TF logo above the four lines can intermittently sit BEHIND a falling payout card. Fix: `z-index: 2` on `.numbers__brand-mark` or move the logo below the falling cards' stacking context.
7. **Dead code cleanup**:
   - Trustpilot widget lazy-load script + CSS in `<head>` (the widget div was removed when featured reviews replaced it)
   - `slick.min.js` (no slider in use anymore)
   - Old `.support-portrait` CSS in mobile-fixes.css (replaced by `.support-quote-card`, kept for backwards compat but no longer referenced)

---

## Conversation Notes (added this continuation)

Reinforcing previous handoff notes + new observations:

- **Jason wants HTML mockup previews BEFORE shipping major changes.** This was reinforced multiple times. Use the preview-HTML pattern (file in `main-site/` root, `*-preview.html` naming) for any section that changes structure or visual treatment. The few times I shipped directly without a preview, he asked for revisions.
- **Honest critique, with one clear recommendation, beats listing 5 options.** Pick one, explain in 1-2 sentences, then offer to ship.
- **AI-generated images are NOT acceptable** for this site. Jason explicitly rejected the AI-generated "Support Person call center.png" because it would undermine the authenticity strategy built everywhere else (real founder, real Trustpilot reviews, real T3 partnership). When in doubt, ask if a photo is real.
- **Section visual iterations are normal — embrace them.** The Human Support section went through ~8 iterations of sizing, positioning, framing, and background treatment before landing on the final quote-card approach. Jason is willing to iterate, just keep each pass focused and explain the change.
- **Compliance language matters.** Two compliance corrections this session: removing "regulated broker-dealer's seat" from journey card 4 (T3 Global isn't the broker-dealer), and adding the asterisk disclaimer on the $1M+ T3 Global Desk card. When making claims about regulatory status, money figures, or trader outcomes, default to honest hedge language.
- **OneDrive truncation kept happening.** Multiple times this session, bash file operations returned stale data because OneDrive's sync was lagging behind. Use the host-filesystem `Read` tool rather than bash `cat` whenever verifying file state.
- **CSS specificity battles are common.** main.css has many rules that conflict with mobile-fixes.css overrides. When a change doesn't seem to apply, the answer is almost always another `!important` or a more specific selector. Use Chrome MCP + `javascript_tool` with `getComputedStyle` to diagnose what's actually winning.

---

*End of continuation handoff. Cache-buster on `mobile-fixes.css` at session end: `?v=202605252700`. Last commit pushed: per Jason's local push commands, may be ahead of or behind the live Vercel deploy depending on whether he ran `git push` after each ship message. Verify with `git log -5` and Vercel deployment dashboard.*
